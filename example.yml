# soapUrl: https://spiredevalpha.fivium.co.uk/spirefox4dev/fox/ispire/SPIRE_OGEL_TYPES
# soapUserName: bisdev.api@test.com
# soapPassword: dev
cacheTimeout: 1000
login: user
password: pass

controlCodeServiceUrl: http://localhost:8090
virtualEuOgelId: OGL61

# spireClientUserName: bisuat.api@test.com
# spireClientPassword: sp1reUAT
# spireClientUrl: https://www.spiretest.trade.gov.uk/spireuat/fox/ispire/
# sspireClientUserName: bisdev.api@test.com
# sspireClientPassword: dev
# sspireClientUrl: https://spiredevalpha.fivium.co.uk/spirefox4dev/fox/ispire/

spireClientUserName: bisuat.api@test.com
spireClientPassword: sp1reUAT
spireClientUrl: https://www.spiretest.trade.gov.uk/spireuat/fox/ispire/

logging:
  level: INFO
  appenders:
    - type: console
      threshold: INFO
      target: stderr
      logFormat: "[%mdc{corrID}] %d [%p] %marker %logger %m%n"
  loggers:
     uk.gov.bis.lite.common.spire.client: TRACE

database:
  # the name of your JDBC driver
  driverClass: org.sqlite.JDBC

  # the username
  #user: pg-user

  # the password
  #password: iAMs00perSecrEET

  # the JDBC URL
  url: jdbc:sqlite:localOgel.db

  # any properties specific to your JDBC driver:
  properties:
    charSet: UTF-8

  # the maximum amount of time to wait on an empty pool before throwing an exception
  maxWaitForConnection: 30s

  # the SQL query to run when validating a connection's liveness
  validationQuery: "/* MyService Health Check */ SELECT 1"

  # the minimum number of connections to keep open
  minSize: 8

  # the maximum number of connections to keep open
  maxSize: 32

  # whether or not idle connections should be validated
  checkConnectionWhileIdle: false

  # the amount of time to sleep between runs of the idle connection validation, abandoned cleaner and idle pool resizing
  evictionInterval: 10s

  # the minimum amount of time an connection must sit idle in the pool before it is eligible for eviction
  minIdleTime: 1 minute


